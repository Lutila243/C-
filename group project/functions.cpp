#include <iostream.h>


using namespace std;


void min_grade (int array[], int length); //before main


int min_grade (int array[20]) {  //actual function finding smallest
 int index;
 int smallIndex;
 int location;
 int temp;
 for (index = 0; index < length - 1; index++){
     smallIndex = index;
     
     for (location = index + 1; location < length; location++) 
     if (list[location < list [smallIndex])
     smallIndex = location;
     
     temp = list(smallIndex];
     list[smallIndex] = list[index];
     list[index] = temp;
     }   
}

min_grade(array, 20); //to call it


int main()
{
    

void printArray(const int x[], 

//infile outfile stuff


cout << "Enter the input File name: " ;
cin >> filename;
infile.open(filename);

cout << "Enter the output file name: ";
cin >> filename;
outfile.open(filename);


system ("pause") ;

return 0;

}
